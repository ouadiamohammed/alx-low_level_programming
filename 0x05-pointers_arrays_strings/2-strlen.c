#include "main.h"

/**
 * _strlen - returns the length of a string.
 *
 * @s: giving string.
 *
 * Return: length of a string.
 */

int	_strlen(char *s)
{
	while (str[i] != '\0')
		i++;

	return (i);
}
